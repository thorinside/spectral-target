name: Build and Release Spectral Target Plugin

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install ARM toolchain
        run: |
          brew update
          brew install --cask gcc-arm-embedded

      - name: Verify installation
        run: arm-none-eabi-g++ --version

      - name: Build plugin
        run: |
          make clean
          make hardware

      - name: Prepare release archive
        run: |
          mkdir -p release/programs/plug-ins/spectral_target
          cp plugins/spectral_target.o release/programs/plug-ins/spectral_target/
          cd release
          zip -r ../spectral_target-plugin.zip programs
          cd ..
          echo "Archive contents:"
          unzip -l spectral_target-plugin.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: spectral_target-plugin.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
